/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    gray: {
      primary: "#D9D9D9",
      secondary: "#8E8E8E",
      try: "#E8E8E8",
      neutral: "#6B7280",
    },
    blue: {
      primary: "#012B55",
    },
  },
});
